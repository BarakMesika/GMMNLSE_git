by BarakM 11.5.24

to propagate a single fiber:

Use the script "BarakTemplate" 

edit "popagation_parameters_template" to your needs (the simulation, fiber and initial pulse parameters)

Run "BarakTemplate" 


TODO: add the gain parameters to the "popagation_parameters_template" and create a script to make only the fiber
for it to be easyer to multi fiber system